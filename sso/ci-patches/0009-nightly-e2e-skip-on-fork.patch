diff --git a/.github/workflows/nightly.yml b/.github/workflows/nightly.yml
index 63484ee7c..6051cf167 100644
--- a/.github/workflows/nightly.yml
+++ b/.github/workflows/nightly.yml
@@ -18,6 +18,9 @@ permissions:
   contents: read
 
 jobs:
+    # Upstream-only: nightly cross-browser E2E is upstream infrastructure and is
+    # not part of the fork's SSO image build. Skipped on any non-upstream repo.
+    if: github.repository == 'Stirling-Tools/Stirling-PDF'
   playwright-all-browsers:
     name: Playwright (chromium + firefox + webkit)
     runs-on: ubuntu-latest
@@ -61,6 +64,10 @@ jobs:
   # Whole-suite accessibility sweep. Pull requests only scan the stories they
   # touch (frontend-a11y.yml) because a full pass takes ~30 minutes; this covers
   # everything else, so a violation introduced by a change somewhere other than
+    # Upstream-only: warms the desktop (Tauri) build cache on upstream's main for
+    # upstream PR/merge-queue builds. The fork ships no desktop app. Skipped on
+    # any non-upstream repo.
+    if: github.repository == 'Stirling-Tools/Stirling-PDF'
   # the story itself — a shared component, a theme token — still surfaces within
   # a day.
   a11y-all-stories:
